steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - beta
    - functions
    - deploy
    - subscribe
    - --stage-bucket
    - selmertsx-sample-bucket
    - --trigger-topic
    - cloud-builds
